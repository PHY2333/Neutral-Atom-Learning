1. **磁场的恢复力矩（内部力矩）：**
    
    当磁矩 $\boldsymbol{\mu}$ 与磁场 $\mathbf{B}$ 夹角为 $\theta$ 时，磁场施加的力矩大小是 $\mu B \sin\theta$。
    
    关键在于，这个力矩的方向是试图把磁矩拉回 $\theta = 0$ 的状态。在极坐标系中，使 $\theta$ 减小的力矩应该带负号，即：
    
    $$\tau_{restoring} = -\mu B \sin\theta$$
    
2. **外加力矩（做功的力矩）：**
    
    我们要计算势能，就相当于计算一个外力把系统从零势能点（$\theta = \pi/2$）准静态地拉到角度 $\theta$ 所做的功。
    
    为了克服磁场的恢复力矩，**外加力矩必须与恢复力矩等大反向**。所以外加力矩是正的（方向指向 $\theta$ 增加的方向）：
    
    $$\tau_{ext} = -\tau_{restoring} = +\mu B \sin\theta$$
    
3. **重新进行做功积分：**
    
    现在我们将正确的 $\tau_{ext}$ 代入积分式。外力做的功等于势能的改变：
    
    $$U(\theta) - U(\pi/2) = \int_{\pi/2}^{\theta} \tau_{ext} \, d\theta' = \int_{\pi/2}^{\theta} (\mu B \sin\theta') \, d\theta'$$
    
    因为 $\int \sin\theta' \, d\theta' = -\cos\theta'$，代入上下限：
    
    $$U(\theta) - 0 = \left[ -\mu B \cos\theta' \right]_{\pi/2}^{\theta}$$
    
    $$U(\theta) = (-\mu B \cos\theta) - (-\mu B \cos(\pi/2))$$
    
    $$U(\theta) = -\mu B \cos\theta - 0$$
    
    $$U(\theta) = -\mu B \cos\theta$$
    

写成矢量的点乘形式，这才严丝合缝地得到了：

$$U = -\boldsymbol{\mu} \cdot \mathbf{B}$$

量子化之后，也就是：

$$\hat{H}_{Z} = -\hat{\boldsymbol{\mu}} \cdot \mathbf{B}$$